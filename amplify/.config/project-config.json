{
    "providers": [
        "awscloudformation"
    ],
    "projectName": "amplify-react-app",
    "version": "3.0",
    "frontend": "javascript",
    "javascript": {
        "framework": "react",
        "config": {
            "SourceDir": "src",
            "DistributionDir": "build",
            "BuildCommand": "yarn run build",
            "StartCommand": "yarn run start"
        }
    }
}